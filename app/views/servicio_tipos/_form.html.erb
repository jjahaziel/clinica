<%= form_for(@servicio_tipo, html: { class: "form-horizontal", role: "form" }) do |f| %>
<% if @servicio_tipo.errors.any? %>
<div class="alert alert-danger alert-dismissable" role="alert">
  <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only"><%= t('close')%></span></button>
  <h4><%= pluralize(@servicio_tipo.errors.count, "error") %> prohibited this servicio_tipo from being saved:</h4>

  <ul>
    <% @servicio_tipo.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>


<div class="form-group">
  <%= f.label t('tipo', :default => 'tipo').humanize, class: "col-sm-2 control-label" %>
  <div class="col-sm-10">
    
    <%= f.text_field :tipo, class: "form-control" %>    
    
  </div>
</div>

<div class="form-group">
  <%= f.label t('descripcion', :default => 'descripcion').humanize, class: "col-sm-2 control-label" %>
  <div class="col-sm-10">
    
    <%= f.text_area :descripcion, class: "form-control" %>    
    
  </div>
</div>
<div class="form-group">
  <div class="col-sm-offset-2 col-sm-10">
    <%= f.submit class: "btn btn-primary" %>
  </div>
</div>
<% end %>
