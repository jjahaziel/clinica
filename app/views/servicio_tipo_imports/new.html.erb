<div class="page-header">
  <%= link_to servicio_tipo_imports_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    <%= t('back')%>
  <% end %>
  <h1><%= t('new')%> <%= t('servicio_tipo_import', :default => 'servicio_tipo_import').humanize %></h1>
</div>

<%= form_for @servicio_tipo_import do |f| %>
<% if @servicio_tipo_import.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@servicio_tipo_import.errors.count, "error") %> prohibited this import from completing:</h2>
	<ul>
		<% @servicio_tipo_import.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>

<p>A CSV or Excel file can be used to import records. The first row should be the column name. The following columns are allowed.</p>

<ul>
	<li><strong> tipo - </strong>String type</li>
	<li><strong> descripcion - </strong>Text type</li>
</ul>

<br>
<div class="form-group">
	<%= f.label :archivo, :class => "col-sm-1 control-label" %>
	<div class="col-sm-4">
		<%= f.file_field :file, {class: "filestyle"} %>
	</div>
</div>
<br>
<br>
<br>
<br>
<%= f.submit :class => "btn btn-primary" %>
<% end %>

