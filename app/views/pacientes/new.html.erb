<div class="page-header">
  <%= link_to pacientes_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    <%= t('back')%>
  <% end %>
  <h1><%= t('new')%> <%= t('paciente', :default => 'paciente').humanize %></h1>
</div>

<%= render 'form' %>
